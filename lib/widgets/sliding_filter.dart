import 'package:flutter/material.dart';

class SlidingFilter extends StatelessWidget {
  const SlidingFilter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
